<div class="tooltip-container">
    <p>
        Autolinks that wraps images in an <code>&lt;a></code> tag with a <code>href</code> to the
        image
        <code>src</code>.
    </p>
    <p>eg. Markdown <code>"![](https://image.src)"</code> is rendered as</p>
    <pre><code
            >&lt;a href="https://image.src">
&lt;img src="https://image.src" />
&lt;/a>
</code></pre>
    <p><i>@default</i><br /><code>true</code></p>
</div>
