<div class="tooltip-container">
    <p>The elements to avoid collapsing whitespaces in.</p>
    <p>
        Set this to <code>"all"</code> to completely preserve whitespaces, or <code>[]</code> to completely
        collapse whitespaces.
    </p>
    <p><i>@default</i><br /><code>["pre"]</code></p>
</div>
