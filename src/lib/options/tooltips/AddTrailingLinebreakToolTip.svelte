<div class="tooltip-container">
    <p>
        Whether to inserts a linebreak inside block-elements that are either empty or end with a
        linebreak.
    </p>
    <p>
        For example, if this is set to false, the HTML-rendering of empty paragraphs/headings might
        have
        <code>0</code>
        height <i>(as it's empty)</i>; and paragraphs/headings ending with a linebreak might not
        render the last linebreak.
    </p>
    <p><i>@default</i><br /><code>false</code></p>
</div>
